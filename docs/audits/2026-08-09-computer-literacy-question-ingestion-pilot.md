# 컴퓨터활용능력 2급 기출문제 내재화 파일럿

- 작업일: 2026-08-09
- 범위: 2020년 7월 4일 필기 교사용 PDF 1회차
- 공개 상태: 비공개 데이터 준비 단계

## 원본

- 전달 파일: `컴퓨터활용능력2급20200704(교사용).pdf`
- 내부 작업 사본: `raw_pdfs/computer-literacy/20200704-teacher.pdf`
- 크기: 293,353 bytes
- SHA-256: `29518812f1f7bc964749add1dab8445485ba51d93b23f23b2f5b5cfe90c2f808`
- PDF: A4 5페이지, 암호화 없음, 교사용 정답 표시와 마지막 1~40번 정답표 포함

`raw_pdfs/`는 기존 `.gitignore` 대상이므로 원본 PDF는 Git 추적 및 공개 배포 대상이 아니다.

## 내재화 결과

| 항목 | 결과 |
|---|---:|
| 전체 문항 | 40 |
| 1과목 컴퓨터 일반 | 20 |
| 2과목 스프레드시트 일반 | 20 |
| 선택지 4개 | 40/40 |
| 정답 범위 1~4 | 40/40 |
| 교사용 본문 정답 표시와 저장 정답 일치 | 40/40 |
| 마지막 페이지 정답표와 저장 정답 일치 | 40/40 |
| 시각자료 직접 확인 대상 | 16 |
| 내재화 PNG | 16 |
| 원본 시각 블록 | 23 |

저장 위치는 `src/data/questions/computer-literacy.json`이며, 기존 확장 URL 설계에서 확정된 `cert_id: computer-literacy`를 사용한다. 문제 ID는 기존 규칙과 같은 `YYYYMMDD_NNN` 형식이다. 신규 챕터·관계·공개 URL은 만들지 않았다.

## 시각자료 대상

다음 문항은 표, 차트, 프로그램 화면, 도형 또는 이미지 선택지를 포함한다.

- `20200704_001`
- `20200704_018`
- `20200704_021`
- `20200704_023`
- `20200704_025`
- `20200704_026`
- `20200704_027`
- `20200704_028`
- `20200704_029`
- `20200704_032`
- `20200704_033`
- `20200704_035`
- `20200704_036`
- `20200704_038`
- `20200704_039`
- `20200704_040`

16문항은 `review: "jpg 확필"`로 표시했다. 이미지 자산은 다음 위치에 문항 ID와 같은 파일명으로 저장했다.

```text
src/assets/questions/computer-literacy/<question_id>.png
```

`20200704_025`, `20200704_027`은 선택지 자체가 표 이미지이므로 선택지를 임의로 텍스트화하지 않고 이미지 선택지 placeholder를 유지했다. 교사용 PDF의 채워진 정답 번호가 이미지에 들어가지 않도록 네 선택지 번호를 모두 열린 번호로 중립 정리했다. 그 밖의 14문항은 원본 PDF의 시각 블록만 추출해 문제 번호, 머리말, 꼬리말과 선택지 정답 표시를 제외했다.

문항별 원본 페이지, 이미지 설명, 자산 경로, 픽셀 크기, SHA-256, 원본 시각 블록 수와 대체 텍스트는 `docs/audits/2026-08-09-computer-literacy-question-image-assets.csv`에 기록했다.

로컬 검수 화면이 같은 정보를 읽을 수 있도록 기계 판독용 이미지 등록부도 다음 위치에 생성했다.

```text
src/data/question-assets/computer-literacy.json
```

## 1회차 로컬 검수 화면

개발 환경에서만 생성되는 관리자 경로에 2020년 7월 4일 40문항 전용 검수 화면을 추가했다.

```text
http://127.0.0.1:4322/admin/computer-literacy-preview/
```

- 1과목 20문항과 2과목 20문항을 분리해 표시한다.
- 문항 카드에서 이미지 포함 여부를 바로 확인할 수 있다.
- 모달은 본문, 이미지, 선택지, 정답 확인 순서로 표시한다.
- 모달 안에서 이전·다음 문제로 이동할 수 있다.
- 25번·27번 이미지 선택지는 이미지 속 열린 번호와 화면 선택지 번호를 일치시켰다.
- 이미지 원본 크기 보기, 정답 상태 초기화, 키보드 좌우 이동을 지원한다.
- 관리자 기출문제 DB 화면에서 검수 화면으로 이동할 수 있다.
- `getStaticPaths()`가 production 빌드에서는 빈 배열을 반환하므로 정적 배포 결과에 경로가 생성되지 않는다.

## 추출 및 정규화

- 2단 편집 PDF의 문항 번호를 좌표로 찾아 문항별 영역을 분리했다.
- 다음 단 또는 다음 페이지로 이어지는 7, 14, 20, 25, 27, 30, 34, 40번을 연결해 선택지 4개를 복원했다.
- 교사용 정답 원문에서 채워진 원문자와 마지막 페이지 정답표를 각각 확인했다.
- PDF 줄바꿈 때문에 분리된 한국어 단어만 원문 의미를 바꾸지 않는 범위에서 결합했다.
- 1번의 본문은 질문 문장만 유지하고 설명 보기 상자는 원본 이미지 자산으로 분리했다.
- 21번의 실행 취소 아이콘은 의미를 바꾸지 않고 `실행 취소` 명령으로 기록했다.

## 공개 차단과 남은 작업

- 컴퓨터활용능력 2급 챕터: 생성하지 않음
- 기출↔챕터 관계: 0건
- 이미지 공개 등록부: 0건
- 내재화 이미지: `src/assets`의 비공개 소스 자산 16건
- production 반영: 없음
- 이용 권리 위험: 해소된 것으로 기록하지 않음

`src/assets`의 파일은 공개 등록부에서 명시적으로 import하지 않는 한 빌드 산출물에 포함되지 않는다. 16문항의 `review: "jpg 확필"`도 그대로 유지해 챕터 관계가 추후 잘못 추가되더라도 현재 공개 필터를 통과하지 못하게 했다.

나머지 회차 ZIP을 받을 때 같은 추출기를 사용하되, 각 회차의 문항 수·정답표·페이지 구성과 시각자료를 다시 검증해야 한다. 이 파일럿만으로 모든 연도 PDF가 같은 편집 구조라고 가정하지 않는다.

## 실행 검증

- `git diff --check`: PASS
- JSON parse: PASS
- 컴활 문항 40개, 고유 ID 40개, 중복 0건: PASS
- 회차 번호 1~40 연속, 과목 배정 20+20: PASS
- 선택지 4개와 정답 범위 1~4: PASS
- 교사용 정답 표시 및 마지막 정답표 대조: 40/40 PASS
- 16개 review ID ↔ 16개 PNG 파일명: PASS
- PNG 디코딩·형식·치수·SHA-256·중복: PASS
- 원본 시각 블록 23개 ↔ 문항 자산 16개: PASS
- 교사용 정답 표시·문제 번호·사이트 머리말·꼬리말 포함: 0건
- 25번·27번 그래픽 선택지의 중립 번호: PASS
- 기존 챕터의 컴활 문제 관계: 0건
- 로컬 검수 화면 HTTP: 200
- 데스크톱 문항 목록·23번 이미지 모달: PASS
- 정답 공개 전 숨김·공개 후 표시: PASS
- 이전·다음 문제 이동: PASS
- 모바일 390×844, 가로 넘침 0: PASS
- 모바일 25번 이미지 선택지·40번 하단 버튼 접근: PASS
- `npm ci`: PASS, 274 packages
- `npm run build`: PASS, 정적 `index.html` 255개
- `npm run check:seo`: PASS
- `npm run check:public-content`: PASS
- 빌드 결과의 컴활 문제 ID·본문 노출: 0건
- 빌드 결과의 `/computer-literacy/` 경로: 생성되지 않음
- 로컬 미리보기: 재시작 후 HTTP 200
