# 에너지관리기능사 기출 전수 감사 후속 조치

- 작업일: 2026-08-05
- 브랜치: `data/energy-management-question-storage`
- 기준 커밋: `9700d63827a2cc07cc428bc94bc4000bd1a23a44`
- 원본 ZIP SHA-256: `488EB053679AA3E489D4B48D815A37D42E7DF659F3E18395F6C20CF81DFCE5D3`
- 선행 감사: `docs/audits/2026-08-05-energy-management-question-full-audit.md`

## 결론

승인된 세 단계를 순서대로 완료했다.

1. 확정 전사 오류 5건을 PDF 기준으로 수정했다.
2. `20140720_014`는 식이 본문에 완전히 들어 있음을 재확인하고 `jpg 확필` 플래그를 제거했다.
3. 남은 이미지·도형·보기 문항 32건을 ID별 PNG로 비공개 소스 자산에 내재화했다.

질문 이미지 스키마와 공개 렌더러는 추가하지 않았다. 32문항의 `review: "jpg 확필"`과 에너지 챕터의 빈 기출 관계를 유지하므로 현재 사이트에는 문항과 이미지가 노출되지 않는다.

## 1. 전사 오류 수정

| ID | 필드 | 수정 전 | 수정 후 | 정답 변경 |
|---|---|---|---|---|
| `20120408_015` | body | `중기난방…관말 중기 트랩` | `증기난방…관말 증기 트랩` | 없음 |
| `20120408_039` | body | `가스버너에서 증류를…` | `가스버너에서 종류를…` | 없음 |
| `20130127_011` | body | `미립회용 1차공기` | `미립화용 1차공기` | 없음 |
| `20151010_023` | body | `전열면적 lm2 당` | `전열면적 1m2 당` | 없음 |
| `20160124_035` | choice 2 | `557.6kca]/m2·h` | `557.6kcal/m2·h` | 없음 |

마지막 두 문항은 기존 JSON의 평문 단위 관례인 `m2`를 유지하고 실제 문자 오류만 수정했다. ID, 자격증, 시험, 과목, 날짜, 번호, 선택지 순서, 정답은 변경하지 않았다.

`20150719_059` 선택지 3의 `산업통상지원부장관`은 실제 명칭과 다르지만 300dpi 확대 결과 PDF도 동일하게 표기돼 있었다. JSON 전사 오류가 아니라 원본 자체 오기이므로 수정하지 않았다.

## 2. `20140720_014` 재판정

PDF 1페이지의 별도 박스는 `(m-1)×100%`만 반복 표시한다. JSON 본문에 같은 식과 질문 문맥이 모두 들어 있고 선택지·정답도 완전하므로 다음 항목만 변경했다.

```text
review: "jpg 확필" → ""
```

중복 PNG는 만들지 않았다.

## 3. 이미지 32건 비공개 내재화

최종 저장 위치:

```text
src/assets/questions/energy-management/<question_id>.png
```

결과:

- PNG 32개
- 질문 ID 32개와 파일명 1:1 일치
- SHA-256 32개 모두 고유
- 손상·해상도 미달·빈 파일 0개
- 교사용 검은 정답 표시 포함 0개
- 문제 번호, 사이트 머리말·꼬리말 포함 0개
- 수식·도형·보기 잘림 0개
- 그래픽 선택지는 열린 번호로 중립 재구성해 정답 노출 제거

문항별 원본 PDF, 페이지, 누락 요소, 픽셀 크기와 SHA-256은 `docs/audits/2026-08-05-energy-management-question-image-assets.csv`에 기록했다.

`src/assets`는 코드에서 import하지 않으면 빌드 산출물로 복사되지 않는다. 의도치 않은 직접 URL 공개를 피하기 위해 `public/`에는 저장하지 않았다.

## 데이터와 공개 상태

| 항목 | 결과 |
|---|---:|
| 에너지관리기능사 문항 | 1,620 |
| 고유 ID | 1,620 |
| 회차 | 27회 × 60문항 |
| 이번 수정 문항 | 5 |
| 이미지 불필요 재판정 | 1 |
| 내재화 PNG | 32 |
| 남은 `jpg 확필` | 32 |
| 에너지 챕터 | 12 |
| 에너지 기출 관계 | 0 |
| 빌드 산출물 내 PNG 파일명 일치 | 0 |
| 빌드 산출물 내 32개 question ID 노출 | 0 |

## 검증 결과

- `git diff --check`: PASS
- JSON parse: PASS
- 전체 문제 수·ID 중복·회차별 1~60번: PASS
- 모든 문항 선택지 4개·정답 범위 1~4: PASS
- 수정 5건의 기대값과 정답 불변: PASS
- `20140720_014` 본문 식·정답·review: PASS
- 32개 review ID ↔ 32개 PNG 파일명: PASS
- PNG 디코딩·형식·치수·SHA-256·중복: PASS
- 에너지 챕터 관계 0건: PASS
- `npm ci`: PASS, 274 packages
- `npm run build`: PASS, 242 pages
- `npm run check:seo`: PASS, HTML 242개·sitemap URL 243개·경고 0·오류 0
- `npm run check:public-content`: PASS, HTML 242개·오류 0
- dist 이미지·question ID 비노출: PASS

`npm ci`에서 `esbuild@0.28.1` 설치 스크립트가 allowScripts에 아직 등록되지 않았다는 경고가 있었지만 설치와 빌드는 성공했다. 신규 의존성은 추가하지 않았다.

## 남은 위험과 다음 게이트

- 이미지 파일은 저장소에만 내재화됐으며 사이트 렌더링 기능은 아직 없다.
- 32문항은 계속 공개 차단 상태다.
- 이후 공개하려면 질문 ID 기반 이미지 로딩, 모바일 레이아웃, 대체 텍스트, 정답 공개 동작을 별도 기술 변경으로 구현하고 검증해야 한다.
- 에너지 기출 관계를 추가하기 전까지 현재 production에는 영향이 없다.
- CBT PDF의 이미지 이용 권리 위험이 해소됐다고 기록하지 않는다. production 공개 전 별도 위험으로 유지한다.
- 이번 로컬 작업은 commit, push, PR, 배포를 수행하지 않았다.
- `AI_HANDOVER.md`는 기술 리뷰와 Owner 승인 전이므로 갱신하지 않았다.
