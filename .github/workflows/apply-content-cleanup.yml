name: Apply public content cleanup

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  parked:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Cleanup workflow parked while the reviewed script is uploaded."
