name: Temporary subject1 handover update

on:
  push:
    branches:
      - docs/industrial-safety-subject1-final-sweep-20260817

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: docs/industrial-safety-subject1-final-sweep-20260817
      - name: Update verified audit and handover
        shell: python
        run: |
          from pathlib import Path

          handover = Path('AI_HANDOVER.md')
          text = handover.read_text(encoding='utf-8')

          replacements = {
            '> **최초 작성일**: 2026-07-14 / **최종 갱신일**: 2026-08-16 / **작성자**: Claude·ChatGPT·Codex / **버전**: v1.19':
            '> **최초 작성일**: 2026-07-14 / **최종 갱신일**: 2026-08-17 / **작성자**: Claude·ChatGPT·Codex / **버전**: v1.20',
            '- **산업안전 관계 상태**: 1,680문항 중 고유 연결 968문항, 미연결 712문항. 관계 참조 978건, 다중 연결 10문항. PR #66의 23건에 이어 PR #68에서 14개 관계를 기존 5개 챕터에 추가했고, 보류 6건은 미연결 상태를 유지한다.':
            '- **산업안전 관계 상태**: 1,680문항 중 고유 연결 **994문항**, 미연결 **686문항**. 관계 참조 **1,004건**, 다중 연결 **10문항**. 1과목은 280문항 중 170문항 연결·110문항 미연결이며, 기존 챕터에 안전하게 추가할 수 있는 P1 후보의 최종 sweep을 완료했다.',
            '- **최신 동적 상태**: 문서 맨 아래의 `산업안전기사 기출 관계 정밀 보강 게이트 완료` 절을 우선한다.':
            '- **최신 동적 상태**: 문서 맨 아래의 `산업안전기사 1과목 P1 관계 보강·최종 sweep 완료` 절을 우선한다.'
          }
          for old, new in replacements.items():
              if text.count(old) != 1:
                  raise SystemExit(f'STOP handover replacement target count={text.count(old)}: {old[:80]}')
              text = text.replace(old, new, 1)

          section_title = '## 산업안전기사 1과목 P1 관계 보강·최종 sweep 완료 (2026-08-17)'
          if section_title in text:
              raise SystemExit('STOP latest handover section already exists')

          section = r'''

---

## 산업안전기사 1과목 P1 관계 보강·최종 sweep 완료 (2026-08-17)

산업안전기사 미연결 문항 중 1과목(P1)을 최신 `main`에서 다시 검토했다. 자동 유사도는 후보 축소에만 사용하고, 문항 의미·현재 챕터 소유 범위·정답·이미지/원문 누락 위험을 직접 대조해 기존 챕터에 정확히 들어가는 관계만 반영했다.

### 1. Batch 3 구현·production 검증

- PR #73: `feat: add subject1 P1 batch3 verified relations`.
- Squash merge commit: `7503b7b9686868019fb60f2cd2c705774b33c67b`.
- 기존 챕터 관계 **6건** 추가.
  - `group-discussion-methods`: 브레인스토밍 5건.
  - `severity-rate`: 일시 전노동 불능상해 1건.
- `severity-rate`의 실제 관계 기준 `examComment`를 **10문항 / 8회차**로 정정하고, `영구 전노동 불능(13급)` 오표기를 **1~3급**으로 수정했다.
- PR head와 squash merge 후 `main`의 Git tree SHA가 `cbbf1224294250250f28436fba25e36828ec244c`로 동일함을 확인했다.
- GitHub Pages run `31964710432`: build **success**, deploy **success**.
- deployment `5934076841`: 최종 상태 **success**, environment URL `https://getpasslab.co.kr/`.

### 2. 검증된 최신 관계 기준선

- 산업안전기사 전체 문항: **1,680**.
- 관계 참조: **1,004**.
- 고유 연결 문항: **994**.
- 미연결 문항: **686**.
- 다중 연결 문항: **10**.
- 1과목 전체 문항: **280**.
- 1과목 고유 연결: **170**.
- 1과목 미연결: **110**.

이 수치는 저장소 관계를 다시 집계한 결과다. 이전 `978 refs / 968 mapped / 712 unmapped` 기준선은 과거 상태이며 현재 동적 정본으로 사용하지 않는다.

### 3. 1과목 기존 챕터 최종 sweep

이전 검토·보류 이력을 제외하고 마지막으로 남은 미검토 10문항을 재추출해 모두 직접 검토했다. **기존 챕터에 추가할 수 있는 안전한 관계는 0건**으로 판정했다.

- `20210515_011`: 재해조사 절차 — 신규 챕터 후보.
- `20210515_014`: 헤링 착시 — **IMAGE_DEPENDENT**, 신규 챕터 후보.
- `20220305_019`: 자동운동 시지각 — 신규 챕터 후보.
- `20210814_012`: 레윈 행동식 — 신규 챕터 후보.
- `20200606_015`: 생체리듬 — 신규 챕터 후보.
- `20180819_006`: 산업재해 기록·분류 — **SOURCE_INCOMPLETE / ORIGINAL_CHECK_REQUIRED**.
- `20190303_010`: 인간오류 독립행동 분류 — 신규 챕터 후보.
- `20180304_020`: 생체리듬 — 신규 챕터 후보.
- `20220305_018`: 바이오리듬 — 신규 챕터 후보.
- `20200926_003`: Y-K 성격검사 — 신규 챕터 후보.

기존 챕터 범위를 다시 대조한 결과 `accident-analysis-tools`는 통계 분석도구, `human-error-by-process`는 인지·판단·조작 과정별 착오요인, `human-relations-mechanism`은 동일화·모방·암시·투사·커뮤니케이션을 소유하므로 위 문항을 유사하다는 이유만으로 흡수하지 않는다.

### 4. 신규 챕터 후보 군집

현재 1과목 미연결 전체에서 관련 키워드를 다시 묶은 결과:

- 레윈 행동법칙: **5문항**.
- 생체리듬: **6문항** — 고전적 23/28/33일 리듬과 주야 생리변화가 섞여 있어 범위 결정 필요.
- 재해조사·재해 발생 조치: **3문항**.
- Y-G / Y-K 성격검사: **2문항**.
- 착시·자동운동: **2문항** — 헤링 문항은 이미지 확인 필요.
- 인간오류 독립행동 분류: **1문항**.

신규 공개 slug는 Owner 결정 범위이므로 이 단계에서는 생성하지 않았다.

### 5. 남은 위험·다음 작업

- `20180819_006`은 현재 JSON 본문에 분류 조건이 빠져 있어 원본 확인 전 관계·신규 콘텐츠 확정 금지.
- `20210515_014`는 그림 선택지이므로 원본 이미지 확인 전 공개 관계 확정 금지.
- 생체리듬 6문항은 하나의 챕터로 합칠지 범위를 나눌지 구조 결정이 필요.
- 신규 공개 식별자 생성은 Owner 승인 후 진행한다.
- 추천 다음 배치는 **레윈 행동법칙 5문항 + 재해조사 3문항 + 성격검사 2문항**의 저위험 신규 챕터 3개이며, 착시·생체리듬·인간오류 분류는 별도 검토를 권장한다.
- `npm ci`의 기존 **2 high severity vulnerabilities** 경고는 관계 작업과 무관하며 아직 별도 해결되지 않았다.

최종 상태: 산업안전기사 1과목의 **기존 챕터 관계 보강 및 P1 최종 sweep VERIFIED**. 다음 구현은 Owner가 신규 공개 챕터 범위를 승인한 뒤 시작한다.
'''
          text = text.rstrip() + section + '\n'
          handover.write_text(text, encoding='utf-8', newline='\n')

          audit = Path('docs/audits/2026-08-17-industrial-safety-subject1-p1-final-sweep.md')
          if audit.exists():
              raise SystemExit('STOP final sweep audit already exists')
          audit.parent.mkdir(parents=True, exist_ok=True)
          audit.write_text(r'''# 산업안전기사 1과목 P1 최종 sweep 감사

- 기준 main: `7503b7b9686868019fb60f2cd2c705774b33c67b`
- 기준 전체 관계: **1,004 refs / 994 unique mapped / 686 unmapped / 10 multi**
- 1과목: **280문항 / 170 mapped / 110 unmapped**
- 목적: 이전 검토 이력을 제외한 마지막 미검토 10문항의 기존 챕터 연결 가능성 재검증
- 기존 챕터 신규 연결: **0건**
- 신규 공개 slug 생성: **0건**

## 1. 3차 QA 결과

### QA 1 — 잔여 후보 재추출

최신 `main`에서 1과목 관계를 재집계하고 이전 Batch 1 보류, Batch 2 직접 검토, Batch 3 직접 검토를 제외했다. 예상대로 마지막 미검토 후보가 **10문항**임을 확인했다.

### QA 2 — 기존 챕터 소유 범위 직접 대조

유사도 상위 챕터 본문을 다시 읽어 문항 의미를 실제로 소유하는지 확인했다. `accident-analysis-tools`, `human-error-by-process`, `human-relations-mechanism`, `carelessness-misjudgment` 등은 각자의 현재 범위가 명확해 최종 10문항을 흡수하면 의미 범위가 깨진다. 따라서 관계 수 증가를 목적으로 강제 연결하지 않는다.

### QA 3 — 미연결 전체의 반복 출제 군집 재확인

최종 10문항만 보지 않고 현재 1과목 전체 미연결 문항에서 키워드 군집을 다시 검색했다. 신규 챕터로 묶을 가치가 있는 반복 출제 주제를 확인했다.

- 레윈 행동법칙: **5문항**
- 생체리듬: **6문항**
- 재해조사·조치: **3문항**
- 성격검사(Y-G/Y-K): **2문항**
- 착시·자동운동: **2문항**
- 인간오류 독립행동 분류: **1문항**

## 2. 마지막 10문항 판정

| question_id | 판정 | 근거 |
|---|---|---|
| `20210515_011` | NEW_CHAPTER_CANDIDATE | 재해조사 방법·원칙 |
| `20210515_014` | IMAGE_DEPENDENT + NEW_CHAPTER_CANDIDATE | 헤링 착시 그림 선택지 |
| `20220305_019` | NEW_CHAPTER_CANDIDATE | 자동운동 시지각 조건 |
| `20210814_012` | NEW_CHAPTER_CANDIDATE | 레윈 `B=f(P·E)` |
| `20200606_015` | NEW_CHAPTER_CANDIDATE | 28일 생체리듬 |
| `20180819_006` | SOURCE_INCOMPLETE / ORIGINAL_CHECK_REQUIRED | 현재 본문에서 괄호 조건 누락 |
| `20190303_010` | NEW_CHAPTER_CANDIDATE | 인간오류 독립행동 분류 |
| `20180304_020` | NEW_CHAPTER_CANDIDATE | 33일 생체리듬 |
| `20220305_018` | NEW_CHAPTER_CANDIDATE | 바이오리듬 판별 |
| `20200926_003` | NEW_CHAPTER_CANDIDATE | Y-K 성격검사 |

## 3. 다음 구현 후보

신규 공개 식별자는 Owner 승인 대상이므로 이번 감사에서는 만들지 않는다.

추천 우선순위는 다음과 같다.

1. 레윈 행동법칙 — 5문항, 이미지·법령 의존 없음.
2. 재해조사·조치 — 3문항, 기존 통계 분석 챕터와 분리된 독립 주제.
3. 성격검사(Y-G/Y-K) — 2문항, 독립 개념으로 범위가 명확함.

생체리듬은 6문항이지만 `23/28/33일 리듬`과 `주간/야간 생리변화`가 섞여 있어 구조 범위를 먼저 결정한다. 착시는 헤링 이미지 문제 때문에 원본 확인 후 진행한다. 인간오류 독립행동 분류는 1문항이라 단독 신규 챕터 효용을 별도 판단한다.

최종 결론: **1과목의 기존 챕터에 안전하게 추가 가능한 P1 관계 후보 검토는 종료**한다. 다음 단계는 신규 챕터 범위에 대한 Owner 승인이다.
''', encoding='utf-8', newline='\n')

      - name: Validate documentation changes
        run: |
          git diff --check
          test "$(git diff --name-only | sort | tr '\n' ' ')" = "AI_HANDOVER.md docs/audits/2026-08-17-industrial-safety-subject1-p1-final-sweep.md "
          grep -F "1,004" AI_HANDOVER.md
          grep -F "994" AI_HANDOVER.md
          grep -F "686" AI_HANDOVER.md
          grep -F "7503b7b9686868019fb60f2cd2c705774b33c67b" AI_HANDOVER.md
      - name: Commit verified documentation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add AI_HANDOVER.md docs/audits/2026-08-17-industrial-safety-subject1-p1-final-sweep.md
          git commit -m "docs: record subject1 P1 final sweep"
          git push origin HEAD:docs/industrial-safety-subject1-final-sweep-20260817
